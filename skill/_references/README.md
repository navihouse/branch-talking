# _references

这个目录**不是 skill**，而是放参考资料的地方（例如规范原文、样例数据、截图）。

- 它没有 `SKILL.md`，因此不会被技能系统发现，也不会出现在技能列表里。
- `package.json` 里把 `_references` 设为 `deny`，是为了明确表达「不要当作技能加载」。

如果你想把它变成一个真正的技能，就在本目录下新建 `SKILL.md`，
但注意技能名必须是小写字母/数字与连字符（下划线不合法），所以目录名也要一起改。
